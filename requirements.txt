# Parliamentary RAG Project Dependencies
# This file list contains the required Python dependencies for both Phase 1 and Phase 2.

# HTTP & scraping
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
playwright>=1.44.0

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Config
pyyaml>=6.0.0

# CLI & output
rich>=13.7.0
tqdm>=4.66.0
click>=8.1.0
fastapi>=0.110.0
uvicorn>=0.28.0

# JSON handling
orjson>=3.10.0

# Numerical, ML & IR (introduced in Phase 2)
numpy>=1.24.0
pandas>=2.0.0
networkx>=3.0.0
matplotlib>=3.7.0
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0
rank-bm25>=0.2.2
pypdf>=5.0.0
# PyMuPDF powers table-aware PDF extraction (src/data/pdf_table_extract.py).
# NOTE: PyMuPDF is AGPL-3.0/commercial dual-licensed. The scraper degrades
# gracefully to pypdf when it is absent, so removing this line is safe.
PyMuPDF>=1.24.0
python-docx>=1.1.0
neo4j>=5.14.0

# torch is required by sentence-transformers (and imported directly by the
# cross-encoder reranker). Install the lightweight CPU build first per
# RUNNING.md to avoid the large CUDA wheel:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
dirty-equals>=0.7.0
